# WikiChat CLI Configuration
# This is a template configuration file.
# Copy this file to ~/.wikichat/config.properties and customize as needed.

# API Configuration
# Base URL of the WikiChat API server
api.url=http://localhost:8080

# API Timeout Configuration (in seconds)
# Connection timeout for establishing connections
api.timeout.connect=30

# Read timeout for reading responses
api.timeout.read=60

# Write timeout for sending requests
api.timeout.write=60

# Output Configuration
# Output format: "text" or "json"
output.format=text

# Enable colored output in terminal
output.colors=true
