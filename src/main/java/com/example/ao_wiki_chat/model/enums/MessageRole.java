package com.example.ao_wiki_chat.model.enums;

/**
 * Role of a message in a conversation.
 */
public enum MessageRole {
    /**
     * Message from the user (question/query)
     */
    USER,
    
    /**
     * Message from the AI assistant (response)
     */
    ASSISTANT
}

